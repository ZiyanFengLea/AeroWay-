Place optional local welcome-screen media files here.

Recommended filenames:

- `ocean.mp4` for the welcome background video
- `ocean-waves.mp3` if you later choose to replace the generated wave ambience

The app currently tries `/media/ocean.mp4` first and then falls back to a remote ocean video source.
